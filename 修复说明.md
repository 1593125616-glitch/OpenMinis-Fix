# OpenMinis 修复版（独立仓库）

本仓库 = 上游 `OpenMinis/OpenMinis`（基线提交 `4ef2900`）+ **13 个修复补丁**。
上游尚未合并（对应 PR：`OpenMinis/OpenMinis#346`）。本仓库是独立快照，可直接在此基础上继续修改。

---

## ① WebView 渲染进程崩溃 → 整个 App 被杀（issue #341）

**现象**
```
Fatal signal 5 (SIGTRAP), code -6 (SI_TKILL)
Abort message: '[FATAL:crashpad_client_linux.cc(667)] Render process (N)'s
crash wasn't handled by all associated webviews, triggering application crash.'
```
不是 SIGSEGV、不是 ANR、不是 Java 异常——是 **Chromium 主动 abort 整个应用进程**。

**根因**
全仓库 **8 个 `WebViewClient` 没有一处实现 `onRenderProcessGone()`**。按 Chromium 契约：渲染进程死亡时，宿主必须处理并 `return true`，否则它判定"无人接管"，直接把主进程一起带走。

**修复**
新增 `ui/webview/WebViewRenderProcess.kt`（`WebView.handleRenderProcessGone()`），并在 8 处 WebView 接线：
`ChatViewModel` / `BrowserUseManager` / `KatexWebViewPool` / `KaTeXView` / `WebViewHolder` / `FilePreviewScreen` / `MinisSkillsBrowserScreen` / `WebAppActivity`。

**触发条件**：WebView 内存涨到 300–480MB 时最容易触发（往 canvas 注入大图是典型引信）。

---

## ② 定时任务静默失效（force-stop 后闹钟全丢）

**根因**
调度本身是 `AlarmManager.setExactAndAllowWhileIdle`，但 `ScheduledTaskManager.rescheduleAll()` **只在 `AlarmReceiver` 的 `BOOT_COMPLETED` 分支被调用过一次**。force-stop（App 更新、部分 ROM 的"清理"、最近任务划掉）会丢掉全部闹钟，且**没有任何补偿路径**——任务就此沉默到下次开机。

**修复**
`MinisApp.onCreate()` 补 `ScheduledTaskManager(this).rescheduleAll()`。

**结论（实测）**：普通进程重启/崩溃**不会**清空闹钟（AlarmManager 由系统持有），只有 force-stop 会。

---

## ③ offload 结果被反复 offload（复环，issue #343）

**根因**
`FileReadTool` 会在结果前面插入头部 `[路径 | 字节 | 行数 | showing a-b of c]` → 回读 offload stub 时判断 `startsWith("[CONTEXT OFFLOADED]")` **恒为 false** → 守卫失效 → stub 被反复再次 offload，无限套娃。

**修复**
新增 `ContextOffload.isOffloadReadback()`，替换裸 `startsWith` 判断。

---

## ④ tool_calls 配对 → Provider 400（issue #342）

```
Provider error: [400] The request is invalid:
An assistant message with 'tool_calls' must be followed by tool messages
responding to each 'tool_call_id'. (Insufficient ...)
```

**机理**：一条带 N 个 `tool_calls` 的 assistant 消息，后面必须跟 **N 条一一对应**且**紧邻**的 tool 结果；少一条、顺序错位、或某条内容为空，供应商都会直接 400 拒绝整个请求，会话卡死在红条上（只能点重试）。

**触发场景**：跨供应商历史（Anthropic/Gemini → OpenAI 兼容降级）、排队用户消息被插到工具回包之间、以及**并行工具调用中有工具返回空**。

**根因**：`ChatViewModel` 的配对修复按 **id 集合**判定（`dropOrphanedToolParts`），能补「整个 id 缺失」，但管不了 Chat Completions 要求的 **紧邻顺序**；而且 iOS 侧的 `OpenAIAgentProvider.sanitizeToolCallAdjacency`（wire 层重排 + 补占位）在 Android 的 `OpenAIProvider` 中**没有对应实现**。

**修复**（`src/android/app/src/main/java/com/openminis/app/provider/openai/OpenAIProvider.kt`）：
移植 iOS `sanitizeToolCallAdjacency`，在 `flattenChatCompletionsMessages` 末尾、`globallyDedupeToolCallIds` 之后调用。两趟处理：
1. 把全部 `role:"tool"` 消息按 `tool_call_id` 收进 map（重复取第一条），从消息流中抽出；顺带把**空白 content 的 tool 回包**替换成非空占位。
2. 重排：逐条发出，遇到带 `tool_calls` 的 assistant 消息就把其回包**紧邻拼接**在后方；缺回包的 `tool_call_id` 注入一条非空占位（`Tool execution result is unavailable (history was truncated or interrupted).`）；无 assistant 认领的孤儿回包丢弃。

新增测试 `src/android/app/src/test/java/com/openminis/app/provider/ToolCallAdjacencyTest.kt`（经 MockWebServer 走真实请求链路，断言出站 `messages` 的角色序列与 `tool_call_id`）。

**注**：`{"error":"tool returned empty"}` 用于空白回包占位，`Tool execution result is unavailable …` 用于缺失回包占位。

---

## ⑤ 持久 shell 超时后会话卡死（issue #358）

**根因**
`PersistentShell` 命令超时时只把 `pendingCallback` 置空，**不结束正在运行的 `/bin/sh`**。持久 shell 在当前命令跑完前不会读下一条 stdin，于是超时那条之后的所有命令都永久排队——该会话的 shell 执行直到被重建前全部失效。

**修复**
（`src/android/app/src/main/java/com/openminis/app/sandbox/PersistentShell.kt`）超时分支在丢弃回调的同时调用 `stop()`，让下一条命令拿到全新的 shell；`ExecutionCoordinator` 会在新 shell 上重新注入环境变量快照。超时返回 exit code `124`。

---

## ⑥ `deepseek-flash` 思考控制失效（issue #356）

**根因**
`ThinkingRuleResolver` 里 DeepSeek 的厂商原生规则（`.deepSeekSibling`，`thinking:{type:enabled|disabled}` 作为 `reasoning_effort` 的根级兄弟字段）用 `*deepseek-v4*` glob 匹配。DeepSeek 现在推荐的 id 是 `deepseek-flash`（由同一 `DeepSeek-V4.1-Flash` 提供），其中不含 `v4-`，因此漏配：既拿不到兄弟字段结构，也无法关闭 thinking，退化到 `openai-compatible-default`。同时 `ThinkingLevelCatalog` 缺 DeepSeek 家族的 ceiling 兜底，未 enrichment 的自定义 endpoint 会发出供应商未声明的字面量 `xhigh`。

**修复**
- `src/ios/Providers/Thinking/ThinkingRuleResolver.swift`：新增 `*deepseek-flash*` 的 `.officialVendor` 规则（`.deepSeekSibling`，label `deepseek-flash-official`），glob 同时覆盖 `deepseek-flash-*` 别名。
- `src/ios/Providers/ThinkingLevelCatalog.swift`：新增 `deepseek-flash` / `deepseek-v4` 家族 → `.high` 兜底 ceiling，与 MiMo/Agnes/seed 同类处理（无 models.dev 数据时不发 `xhigh`）。
- `src/ios/MinisTests/ThinkingWireGoldenSnapshotTests.swift`、`ThinkingRulesRegressionTests.swift`：补 `deepseek-flash` / `deepseek-flash-unified` 矩阵行与 5 个回归用例。

---

## ⑦ Cerebras 第二轮起 400：历史带 `reasoning_content`（issue #361）

```
HTTP 400: messages.2.assistant.reasoning_content: property
'messages.2.assistant.reasoning_content' is unsupported
（code: wrong_api_format）
```

**根因**
Cerebras 的 Chat Completions 消息 schema 不接受 `messages[].assistant.reasoning_content`。首轮成功，第二轮起只要历史里带着上一轮捕获的推理内容就整体 400。这是 **schema 约束而非能力约束**，无法从模型元数据推断，只能按 endpoint 识别厂商——与 Mistral（`isMistral`）完全同类。

**修复**
把 Cerebras 加入「禁止回显 `reasoning_content`」的厂商判定（`forbidReasoningField`），沿用与 Mistral 相同的 base URL 匹配谓词：
- `src/ios/Providers/OpenAI/OpenAIProvider.swift`：新增 `isCerebras`（`customBaseURL` 小写后含 `cerebras`）。
- `src/ios/Providers/OpenAI/OpenAIAgentProvider.swift`：`forbidReasoningField = provider.isMistral || provider.isCerebras`。
- `src/android/app/src/main/java/com/openminis/app/provider/openai/OpenAIProvider.kt`：新增 `isCerebras`（`basePath` 小写后含 `cerebras`），`forbidReasoningField = isMistral || isCerebras`。

该改动**只抑制历史回显**，不改变请求级 thinking 形状（Cerebras 原生接受根级 `reasoning_effort`，模型仍可正常开启思考）。

新增测试：
- Android `src/android/app/src/test/java/com/openminis/app/provider/CerebrasReasoningFieldTest.kt`（经 MockWebServer 走真实链路，含大小写与负向对照）。
- iOS `src/ios/MinisTests/CerebrasReasoningHistoryTests.swift`（直接调用 `flattenChatCompletionsMessages`）。

---

## 构建说明（本仓库**未经编译验证**）

- JDK **17**，Gradle **8.11.1**（wrapper），AGP **8.7.3**，Kotlin **2.1.0**
- Android SDK：compileSdk **36** / targetSdk **35** / minSdk **26**；CMake **3.22.1**；**NDK r28+**
- 需先用 NDK 构建 `deps/proot`（fork），产物放入 app 的 `assets/`——原生构建不可避免
- 本仓库**没有 CI**，全部补丁均**未经编译验证**，请先在本地跑通 `./gradlew assembleDebug`
- Android 侧新增/受影响单测：`app/src/test/java/com/openminis/app/provider/ToolCallAdjacencyTest.kt`、`CerebrasReasoningFieldTest.kt`、`app/src/test/java/com/openminis/app/data/CompactToolPairingTest.kt`

---

## ⑧ Android label 输入框弹出密码键盘（issue #364 Android）

**根因**
`ProviderDetailScreen.kt` 的 label 字段调用 `SectionTextField` 时未显式设置 `keyboardOptions`，Android 的 AutoFill 系统可能将其识别为"用户名/密码配对"中的用户名，触发密码键盘。

**修复**
为 label 的 `SectionTextField` 添加 `keyboardOptions = KeyboardOptions(keyboardType = KeyboardType.Text)`，并补充对应 import。iOS 侧已有此修复（`T-provider-label-keyboard`）。

---

## ⑨ Android 模型显示名称保留尾部空格（issue #365 Android）

**根因**
`ModelEntryDetailScreen.kt` 保存 displayName 时用 `displayName.trim()`，会把用户特意保留的尾部空格（如 `GPT-5 ` 用于对齐列表）直接吃掉，与 iOS 行为不一致（iOS `T-custom-model-name-trailing-space` 保留空格）。

**修复**
改为先判断全空白才视为空，非空时保留原始值（含尾部空格），仅在值与 baseModel.displayName 相同时才存 null（表示无覆盖）。

---

## ⑩ 备份"删除记录和文件"点击后无变化（issue #367）

**根因**
`removeHistoryRecordWithFiles` 是异步操作（需要遍历所有远端目的地删除包），但调用方在启动协程后立即 `popBackStack()` 导航走，用户被弹出详情页后看不到删除结果——记录行还在原地，直到下次重新进入页面才消失（或永远不消失，如果 rclone 操作失败被 swallow）。

**修复**
将 `removeHistoryRecordWithFiles` 改为 `suspend fun`，导航层用 `rememberCoroutineScope().launch { ... }` 等待完成后再 pop，确保 UI 状态已更新。

---

## ⑪ Android 模型名显示错误 + iOS 备份恢复到 Android 产生重复（issue #366）

**根因**
备份恢复时（`ProviderRepository.restoreBackupModelEntries`），`displayName` 直接取 JSON 中的值：`m.optString("displayName", modelId)`。当 iOS 备份的模型没有自定义 displayName 时，backup 里存的是原始 ID（如 `deepseek-v4-flash`），恢复到 Android 后原样显示，而 Android 自己的模型列表会通过 `modelDisplayName()` 格式化为 `DeepSeek V4 Flash`——两套命名规则并存导致同一模型出现两个名字。

**修复**
恢复时若 `displayName == modelId`（未被自定义过），调用 `LLMModel.ModelMeta.modelDisplayName(modelId)` 格式化后再存入，保证 iOS↔Android 双向恢复后显示一致。

---

## ⑫ Claude OAuth token 请求被 Cloudflare 拦截（issue #360）

**根因**
Claude OAuth 流程中，`postTokenRequest`（iOS）和 `exchangeCodeJson`（Android）仅发送 `x-api-key` + `Content-Type`，缺少 Claude Code CLI 的指纹头（`anthropic-beta` beta flags + `User-Agent: claude-cli/...`）。Anthropic OAuth 后端通过 UA + Stainless headers 组合判断客户端身份，缺失则返回 403，OAuth 流程卡死。

**修复**
- iOS `ClaudeOAuthManager.postTokenRequest`：补发 `anthropic-version`、`anthropic-beta`（合并自 `ClaudeCodeMimicry.betaFlags`）、`User-Agent: claude-cli/2.1.195 (external, cli)`。
- Android `ClaudeOAuthManager.exchangeCodeJson`：同上。
- iOS `OAuthHTTPClient`：提取 `ClaudeCodeMimicry` enum，统一维护 beta flags、UA、endpoint URL，避免两处漂移。
- tokenURL 统一改为 `claude.ai/v1/oauth/token`（解决 #243 的计费降级问题）。

---

## ⑬ Cerebras reasoning_content 历史回显导致 400（issue #361，已在上方 ⑦ 详述）

— 见上方 ⑦。

---

## 分支说明

| 分支 | 内容 |
|---|---|
| `main` | **修复版**（上游 `4ef2900` + 13 个补丁） |
| `upstream` | 上游原版快照，用于 diff 对比 |
